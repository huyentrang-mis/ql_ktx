<?php
require_once __DIR__ . '/../includes/functions.php';
$user = currentUser();
$unreadCount = $user ? getUnreadCount($user['id']) : 0;
$currentPage = basename($_SERVER['PHP_SELF'], '.php');
?>
<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title><?= SITE_NAME ?></title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@300;400;500;600;700;800&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
<link href="<?= BASE_URL ?>/assets/css/style.css" rel="stylesheet">
</head>
<body>

<?php if (isLoggedIn()): ?>
<!-- SIDEBAR -->
<div class="sidebar" id="sidebar">
    <div class="sidebar-brand">
        <div class="brand-icon"><i class="bi bi-building-fill"></i></div>
        <div class="brand-text">
            <span class="brand-name">KTX Smart</span>
            <span class="brand-sub">Ký Túc Xá</span>
        </div>
    </div>
    
    <div class="sidebar-user">
        <div class="user-avatar">
            <span><?= mb_substr($user['full_name'], 0, 1) ?></span>
        </div>
        <div class="user-info">
            <span class="user-name"><?= htmlspecialchars($user['full_name']) ?></span>
            <span class="user-role"><?= ['admin'=>'Quản trị viên','staff'=>'Nhân viên','student'=>'Sinh viên'][$user['role']] ?></span>
        </div>
    </div>

    <nav class="sidebar-nav">
        <div class="nav-section-label">TỔNG QUAN</div>
        <a href="<?= BASE_URL ?>/pages/dashboard.php" class="nav-item <?= $currentPage==='dashboard'?'active':'' ?>">
            <i class="bi bi-grid-1x2-fill"></i><span>Dashboard</span>
        </a>
        
        <?php if (in_array($user['role'], ['admin','staff'])): ?>
        <div class="nav-section-label">QUẢN LÝ</div>
        <a href="<?= BASE_URL ?>/pages/students.php" class="nav-item <?= $currentPage==='students'?'active':'' ?>">
            <i class="bi bi-people-fill"></i><span>Sinh Viên</span>
        </a>
        <a href="<?= BASE_URL ?>/pages/rooms.php" class="nav-item <?= $currentPage==='rooms'?'active':'' ?>">
            <i class="bi bi-door-closed-fill"></i><span>Phòng Ở</span>
        </a>
        <a href="<?= BASE_URL ?>/pages/invoices.php" class="nav-item <?= $currentPage==='invoices'?'active':'' ?>">
            <i class="bi bi-receipt-cutoff"></i><span>Hóa Đơn</span>
        </a>
        <a href="<?= BASE_URL ?>/pages/payments.php" class="nav-item <?= $currentPage==='payments'?'active':'' ?>">
            <i class="bi bi-credit-card-fill"></i><span>Thanh Toán</span>
        </a>
        <a href="<?= BASE_URL ?>/pages/utilities.php" class="nav-item <?= $currentPage==='utilities'?'active':'' ?>">
            <i class="bi bi-lightning-charge-fill"></i><span>Điện - Nước</span>
        </a>
        <?php endif; ?>
        
        <?php if ($user['role'] === 'student'): ?>
        <div class="nav-section-label">CÁ NHÂN</div>
        <a href="<?= BASE_URL ?>/pages/my_invoices.php" class="nav-item <?= $currentPage==='my_invoices'?'active':'' ?>">
            <i class="bi bi-receipt-cutoff"></i><span>Hóa Đơn Của Tôi</span>
        </a>
        <a href="<?= BASE_URL ?>/pages/my_room.php" class="nav-item <?= $currentPage==='my_room'?'active':'' ?>">
            <i class="bi bi-house-fill"></i><span>Phòng Của Tôi</span>
        </a>
        <?php endif; ?>
        
        <?php if ($user['role'] === 'admin'): ?>
        <div class="nav-section-label">HỆ THỐNG</div>
        <a href="<?= BASE_URL ?>/pages/users.php" class="nav-item <?= $currentPage==='users'?'active':'' ?>">
            <i class="bi bi-person-badge-fill"></i><span>Tài Khoản</span>
        </a>
        <a href="<?= BASE_URL ?>/pages/services.php" class="nav-item <?= $currentPage==='services'?'active':'' ?>">
            <i class="bi bi-gear-fill"></i><span>Dịch Vụ</span>
        </a>
        <a href="<?= BASE_URL ?>/pages/backup.php" class="nav-item <?= $currentPage==='backup'?'active':'' ?>">
            <i class="bi bi-cloud-arrow-up-fill"></i><span>Sao Lưu</span>
        </a>
        <a href="<?= BASE_URL ?>/pages/logs.php" class="nav-item <?= $currentPage==='logs'?'active':'' ?>">
            <i class="bi bi-journal-text"></i><span>Nhật Ký</span>
        </a>
        <?php endif; ?>
        
        <div class="nav-section-label">TÀI KHOẢN</div>
        <a href="<?= BASE_URL ?>/pages/profile.php" class="nav-item <?= $currentPage==='profile'?'active':'' ?>">
            <i class="bi bi-person-circle"></i><span>Hồ Sơ</span>
        </a>
        <a href="<?= BASE_URL ?>/pages/notifications.php" class="nav-item <?= $currentPage==='notifications'?'active':'' ?>">
            <i class="bi bi-bell-fill"></i><span>Thông Báo</span>
            <?php if ($unreadCount > 0): ?><span class="badge-count"><?= $unreadCount ?></span><?php endif; ?>
        </a>
        <a href="<?= BASE_URL ?>/pages/logout.php" class="nav-item nav-logout">
            <i class="bi bi-box-arrow-right"></i><span>Đăng Xuất</span>
        </a>
    </nav>
</div>

<!-- TOPBAR -->
<div class="topbar">
    <button class="sidebar-toggle" id="sidebarToggle"><i class="bi bi-list"></i></button>
    <div class="topbar-breadcrumb">
        <span><?= SITE_NAME ?></span>
    </div>
    <div class="topbar-actions">
        <a href="<?= BASE_URL ?>/pages/notifications.php" class="topbar-btn">
            <i class="bi bi-bell-fill"></i>
            <?php if ($unreadCount > 0): ?><span class="notif-dot"></span><?php endif; ?>
        </a>
        <a href="<?= BASE_URL ?>/pages/profile.php" class="topbar-btn">
            <i class="bi bi-person-circle"></i>
        </a>
    </div>
</div>

<!-- MAIN CONTENT WRAPPER -->
<div class="main-wrapper">
<?php endif; ?>